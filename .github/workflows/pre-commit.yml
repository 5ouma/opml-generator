name: 🚸 pre-commit

on:
  push:
    branches:
      - main
  pull_request:

permissions: {}

jobs:
  pre-commit:
    name: 🚸 pre-commit
    runs-on: Ubuntu-Latest
    timeout-minutes: 5

    steps:
      - name: 🚸 Run pre-commit
        uses: 5ouma/utils/pre-commit@2823e679f753e4f03cd986c2f610757cfaecfd44 # v0.9.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
