name: 🚸 pre-commit

on:
  push:
    branches:
      - main
  pull_request:

permissions: {}

jobs:
  pre-commit:
    name: 🚸 pre-commit
    runs-on: Ubuntu-Slim
    timeout-minutes: 5

    steps:
      - name: 🚸 Run pre-commit
        uses: 5ouma/utils/pre-commit@f4ffd21171133eb7f4731b7a486daa153cd355a3 # v0.8.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
