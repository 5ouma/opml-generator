name: 🚸 pre-commit

on:
  push:
    branches:
      - main
  pull_request:

permissions: {}

jobs:
  pre-commit:
    name: 🚸 pre-commit
    runs-on: Ubuntu-Slim
    timeout-minutes: 5

    steps:
      - name: 🚸 Run pre-commit
        uses: 5ouma/utils/pre-commit@da6ac93770103bb5e35b8fae7e213628e324eb7a # v0.8.3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
