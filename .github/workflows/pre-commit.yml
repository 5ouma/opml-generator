name: 🚸 pre-commit

on:
  push:
    branches:
      - main
  pull_request:
  workflow_call:

permissions: {}

jobs:
  pre-commit:
    runs-on: Ubuntu-Latest
    timeout-minutes: 3

    steps:
      - name: 🚸 Run pre-commit
        uses: 5ouma/utils/pre-commit@b4cff9e76103ffe4d14c9812f9a2262b7994a3b3 # v0.2.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
