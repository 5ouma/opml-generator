name: 🚸 pre-commit

on:
  push:
    branches:
      - main
  pull_request:
  workflow_call:

permissions: {}

jobs:
  pre-commit:
    runs-on: Ubuntu-Latest
    timeout-minutes: 3

    steps:
      - name: 🚸 Run pre-commit
        uses: 5ouma/utils/pre-commit@e9f6b5e242c44ae969f2f39d11aee713d1ac7055 # v0.2.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
