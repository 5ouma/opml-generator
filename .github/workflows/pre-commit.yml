name: 🚸 pre-commit

on:
  push:
    branches:
      - main
  pull_request:

permissions: {}

jobs:
  pre-commit:
    name: 🚸 pre-commit
    runs-on: Ubuntu-Slim
    timeout-minutes: 5

    steps:
      - name: 🚸 Run pre-commit
        uses: 5ouma/utils/pre-commit@1409b1a854917a5e6a05730772f815534e671981 # v0.8.2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
