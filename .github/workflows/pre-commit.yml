name: 🚸 pre-commit

on:
  push:
    branches:
      - main
  pull_request:
  workflow_call:

permissions: {}

jobs:
  pre-commit:
    runs-on: Ubuntu-Latest
    timeout-minutes: 3

    steps:
      - name: 🚸 Run pre-commit
        uses: 5ouma/utils/pre-commit@35e2cccab72316555c4c01855a8ebebffeb45aa2 # v0.4.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
