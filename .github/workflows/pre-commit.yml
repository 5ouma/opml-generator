name: 🚸 pre-commit

on:
  push:
    branches:
      - main
  pull_request:

permissions: {}

jobs:
  pre-commit:
    name: 🚸 pre-commit
    runs-on: Ubuntu-Latest
    timeout-minutes: 3

    steps:
      - name: 🚸 Run pre-commit
        uses: 5ouma/utils/pre-commit@6968ce42d417d90e4a8e94e633e76f67be5b5acd # v0.4.7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
