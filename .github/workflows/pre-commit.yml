name: 🚸 pre-commit

on:
  push:
    branches:
      - main
  pull_request:

permissions: {}

jobs:
  pre-commit:
    name: 🚸 pre-commit
    runs-on: Ubuntu-Slim
    timeout-minutes: 5

    steps:
      - name: 🚸 Run pre-commit
        uses: 5ouma/utils/pre-commit@5d64e87de2de9392e2a9df45d487a0c54194d281 # v0.7.2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
